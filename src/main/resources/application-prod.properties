server.error.whitelabel.enabled=false
spring.devtools.add-properties=false
logging.level.root=warn
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.open-in-view=false
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://${DB_ENDPOINT}/springdatabase
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
server.port=8585
socket.port=8589
socket.host=0.0.0.0
spring.session.timeout=30m
server.servlet.session.cookie.name=sess_java_spring
jwt.secret=bmlnaHRoYXdrY29kaW5nc29jaWV0eWFwY29tcHV0ZXJzY2llbmNlYQ==
google.api.key=AIzaSyC1xyYXgYfGRJrvMOY8iT2bZnb7-Nyd1HY
spring.main.allow-bean-definition-overriding=true
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dnhsbathroom@gmail.com
spring.mail.password=vjor zncp naam daph
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# IMPORTANT: For first-time deployment on empty database, use 'create' to initialize schema
# After first successful deployment, change to 'update' to preserve data on subsequent restarts
# Using 'create' will drop and recreate all tables on each startup - only use for initial setup
# 
# If you get errors about dropping non-existent constraints, clean the database first:
# DROP DATABASE springdatabase; CREATE DATABASE springdatabase;
spring.jpa.hibernate.ddl-auto=create
spring.jpa.properties.hibernate.hbm2ddl.jdbc_metadata_extraction_strategy=individually
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.globally_quoted_identifiers=false
spring.jpa.properties.hibernate.hbm2ddl.create_namespaces=true
# Note: If you get errors about dropping non-existent constraints, 
# manually drop all tables in the database first, or use a clean database
# Example SQL: DROP DATABASE springdatabase; CREATE DATABASE springdatabase;

spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.validation-timeout=3000
spring.datasource.hikari.max-lifetime=1800000

spring.config.import=optional:file:.env[.properties]

security.rate-limit.requests-per-minute=1000

